services:
  redis:
    image: redis:alpine
    container_name: mi-redis
  web:
    build: .
    container_name: flask-web
    ports:
      - "5000:5000"
    environment:
      - APP_NAME=${APP_NAME}
      - APP_VERSION=${APP_VERSION}
      - REDIS_HOST=${REDIS_HOST}
    depends_on:
      - redis