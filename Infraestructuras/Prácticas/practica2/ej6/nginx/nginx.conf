server {
    listen 80;
    
    location /api/ {
        proxy_pass http://api:5000/api/;
        proxy_set_header Host $host;
    }
    
    location / {
        return 200 'Frontend funcionando. Usa /api/data para consultar el backend.\n';
        add_header Content-Type text/plain;
    }
}
