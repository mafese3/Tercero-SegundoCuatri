services:
  db:
    image: postgres:15
    container_name: mi-postgres
    environment:
      - POSTGRES_PASSWORD=secretpassword    
  web:
    image: adminer
    container_name: mi-adminer
    ports:
      - "8080:8080"
    depends_on:
      - db